local life_state = require 'src/conways_game_of_life/life_state_enum'

local function get_next_state(cell, turns)
  return li
end

return function()
    return {
    life_state = life_state.alive
  }
end
